apiVersion: apps/v1
kind: Deployment
metadata:
  name: resnet50-inference
  namespace: default
  labels:
    app: resnet50
    workload: ai-inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resnet50
  template:
    metadata:
      labels:
        app: resnet50
        workload: ai-inference
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      runtimeClassName: nvidia
      containers:
      - name: resnet50
        image: hamidhrf/resnet50-inference:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: metrics
          protocol: TCP
        resources:
          limits:
            nvidia.com/gpu: 1
          # No CPU/Memory limits - allow workload to use resources freely
          
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: resnet50-inference
  namespace: default
  labels:
    app: resnet50
spec:
  type: ClusterIP
  selector:
    app: resnet50
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: metrics